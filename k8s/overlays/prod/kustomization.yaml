apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod
resources:
  - ../../base
  - namespace.yaml
# Pin a real tag on release: kustomize edit set image jarvis-web=jarvis-web:<sha>
images:
  - name: jarvis-web
    newTag: latest
