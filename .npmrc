; Mirrors web/.npmrc so a workspace-root install hoists these the same way a
; `pnpm install` inside web/ used to. ESLint and Prettier resolve plugins from
; the package that invokes them, so they have to be visible at the top level.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
